//@ sourceMappingURL=environment.map
// Generated by CoffeeScript 1.6.1
var Environment;

Environment = (function() {
  var DEVELOPMENT_PORT;

  DEVELOPMENT_PORT = process.env['GATEWAY_PORT'] || '3000';

  Environment.Development = new Environment('localhost', DEVELOPMENT_PORT, false);

  Environment.Sandbox = new Environment('api.sandbox.braintreegateway.com', '443', true);

  Environment.Production = new Environment('api.braintreegateway.com', '443', true);

  function Environment(server, port, ssl) {
    this.server = server;
    this.port = port;
    this.ssl = ssl;
  }

  return Environment;

})();

exports.Environment = Environment;
